language: ruby
rvm:
- 2.4.1

script: 
- bundle install
- bundle exec jekyll build

before_deploy:
  - chmod +x deploy.sh

deploy:
  provider: script
  script: ./deploy.sh
  skip_cleanup: true


sudo: false

cache: bundler